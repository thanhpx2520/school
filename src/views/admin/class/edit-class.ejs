<!DOCTYPE html>
<html lang="en">
<%- include('./../partial/head') %>

<body>
  <%- include('./../partial/header') %>
  <main>
    <div class="d-flex">
      <div class="row mx-0">
        <%- include('./../partial/nav', { navLinkActive: 'Classes' }) %>
        <!-- CONTENT -->
        <div class="fix-content">
          <div class="mx-5">
            <div class="row">
              <div class="col">
                <nav aria-label="breadcrumb">
                  <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/admin/dashboard">Dashboard</a></li>
                    <li class="breadcrumb-item"><a href="/admin/classes">Quản lý lớp học</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Chi tiết lớp học / <%= classItem.doc.tenLop %></li>
                  </ol>
                </nav>
                <h3>Chi tiết lớp học: <%= classItem.doc.tenLop %></h3>
                <a class="mt-2 btn btn-primary" href="/admin/classes" role="button">Danh sách</a>
              </div>
            </div>
          </div>
          <!-- alert -->
          <div class=" mt-3 fix-mr">
            <div class="row">
              <% if (classAlert && classAlert.error) { %>
              <div class='p-2 alert alert-danger col-6'><%= classAlert.error %></div>
              <% } %>
            </div>
          </div>
          <!-- alert -->
          <div class="mx-5 mt-3">
            <form method="post" class="row">
              <div class="col-6">
                <div class="form-group">
                  <label for="categoryName">Tên lớp học</label>
                  <input type="text" class="form-control" id="categoryName" name="tenLop" value="<%= classItem.doc.tenLop %>" placeholder="Nhập tên lớp học" required />
                </div>
              </div>
              <div class="col-6"></div>
              <button type="submit" class="fix-btn-submit mt-2 btn btn-warning w-auto">Sửa lớp học</button>
            </form>
          </div>
        </div>
      </div>
    </div>
  </main>
  <%- include('./../partial/footer') %>
</body>

</html>